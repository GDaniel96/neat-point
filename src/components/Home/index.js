import react from "react";
import styled from "styled-components";
import { Col, Row, Container } from "reactstrap";

const Home = () => {
  return (
    <Container>
      <Row></Row>
    </Container>
  );
};

export default Home;
